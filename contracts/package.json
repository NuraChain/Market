{
    "name": "auctionhouse-contracts",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "description": "On-chain CPMM prediction markets for AuctionHouse (Cosmos EVM)",
    "scripts": {
        "compile": "hardhat compile",
        "test": "hardhat test",
        "coverage": "hardhat coverage",
        "node": "hardhat node",
        "deploy:local": "hardhat run scripts/deploy.ts --network localhost",
        "deploy": "hardhat run scripts/deploy.ts --network cosmosEvm",
        "seed": "hardhat run scripts/seed.ts --network localhost",
        "export-abis": "hardhat run scripts/export-abis.ts"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox-mocha-ethers": "^3.0.7",
        "@openzeppelin/contracts": "^5.6.1",
        "@openzeppelin/contracts-upgradeable": "^5.6.1",
        "@types/node": "^26.1.2",
        "dotenv": "^17.4.2",
        "hardhat": "^3.12.0",
        "typescript": "^7.0.2"
    }
}
