import { client, PERIODS, type Period } from '../api.ts';
import { pageOf } from '../lib/paged.ts';

import { useLocale } from '../stores/locale.store.ts';

import { formatMoney, formatVolume } from '../i18n/format.ts';

import Icon from '../icons/icon.component.azeroth';

import Tabs from '../components/ui/tabs.component.azeroth';
import SkeletonList from '../components/ui/skeleton-list.component.azeroth';
import Pagination from '../components/ui/pagination.component.azeroth';
import Tooltip from '../components/ui/tooltip.component.azeroth';
import EmptyState from '../components/ui/empty-state.component.azeroth';
import { cardClass } from '../components/ui/variants.ts';

const PAGE_SIZE = 10;

export default component Leaderboard()
{
    const { t, lang } = useLocale();

    state period: Period = 'week';
    state page = 1;

    resource rows = (active: Period) => client.leaderboard.list({ query: { period: active } }) with { source: period };

    derived periodTabs = PERIODS.map((entry) => ({ id: entry, label: t(`leaderboard.${ entry }`) }));
    derived paged = pageOf(rows.data() ?? [], page, PAGE_SIZE);

    <section class="shell py-5">
        <div class="mx-auto max-w-3xl">
            <h1 class="mb-1 flex items-center gap-2 text-2xl font-bold tracking-tight motion-safe:animate-rise">
                <Icon name="trophy" size={ 22 } class="text-gold" />
            { t('leaderboard.title') }
            </h1>

            <div class="mb-4 mt-4">
                <Tabs tabs={ periodTabs } active={ period } onChange={ (next) => { period = next as Period; page = 1; } } />
            </div>

            <Show when={ rows.loading() }>
                <SkeletonList count={ 5 } height="h-14" radius="card" gap="md" />
            </Show>

            <Show when={ !rows.loading() && paged.rows.length === 0 }>
                <EmptyState icon="trophy" title={ t('leaderboard.empty') } hint={ t('leaderboard.emptyHint') } />
            </Show>

            <Show when={ !rows.loading() && paged.rows.length > 0 }>
                <ol class="grid grid-cols-1 gap-2.5">
                    <For each={ paged.rows } key={ (row) => `${ period }-${ row.rank }` }>
                    { (row) =>
                        <li class="motion-safe:animate-rise">
                            <div class={ `${ cardClass({ tone: row.name === 'IntelligentQuantum' ? 'brand' : 'line', padding: 'snug' }) } flex items-center gap-3` }>
                                <Show
                                    when={ row.rank <= 3 }
                                    fallback={
                                        <span class="flex h-9 w-9 shrink-0 items-center justify-center" aria-hidden="true">
                                            <span class="nums text-[14px] font-bold text-faint">{ row.rank }</span>
                                        </span>
                                    }
                                >
                                    <Tooltip label={ `${ t('leaderboard.rank') } ${ row.rank }` }>
                                        <span class="flex h-9 w-9 shrink-0 items-center justify-center" role="img" aria-label={ `${ t('leaderboard.rank') } ${ row.rank }` }>
                                            <Show when={ row.rank === 1 }><Icon name="crown" size={ 22 } class="text-gold" /></Show>
                                            <Show when={ row.rank === 2 || row.rank === 3 }><Icon name="medal" size={ 20 } class={ row.rank === 2 ? 'text-muted' : 'text-gold' } /></Show>
                                        </span>
                                    </Tooltip>
                                </Show>
                                <span class="flex h-9 w-9 shrink-0 items-center justify-center rounded-full bg-overlay text-[13px] font-bold text-muted" aria-hidden="true">{ row.name.slice(0, 1).toUpperCase() }</span>
                                <span class="min-w-0 flex-1">
                                    <span class="block truncate text-[14px] font-bold">{ row.name }</span>
                                    <span class="nums block text-[12px] text-faint">{ t('market.volume') }: { formatVolume(row.volume, lang()) }</span>
                                </span>
                                <span class="nums shrink-0 text-[15px] font-bold text-yes">+{ formatMoney(row.profit, lang(), { compact: true }) }</span>
                            </div>
                        </li>
                    }
                    </For>
                </ol>
                <div class="mt-5">
                    <Pagination page={ paged.current } pages={ paged.pages } onChange={ (next) => page = next } />
                </div>
            </Show>
        </div>
    </section>
}
